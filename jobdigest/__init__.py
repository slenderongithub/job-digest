"""jobdigest — a personal, read-only job aggregator that scrapes open roles
from India-relevant sources, filters them to what you're plausibly eligible for,
ranks the survivors with an LLM, and writes a local HTML digest of only the new
matches. No auto-apply, no data sent anywhere off your machine."""

__version__ = "0.1.0"
